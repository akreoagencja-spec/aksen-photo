# Aksen Photo — staging deploy marker

This file exists only to trigger and document the first Vercel Preview deployment after connecting the GitHub repository.

Rules:
- production WordPress remains untouched,
- production database remains untouched,
- DNS remains unchanged,
- this branch is for preview/staging only,
- staging must remain noindex until controlled cutover.
